pre-commit:
  parallel: true
  commands:
    format-mdx:
      glob: "*.{md,mdx}"
      run: pnpm dlx @takazudo/mdx-formatter --write {staged_files} && git add {staged_files}
    format-astro:
      glob: "*.astro"
      run: pnpm exec prettier --write {staged_files} && git add {staged_files}
